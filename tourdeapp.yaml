# $schema: https://tourde.cloud/api/v2/schema

exposedPort: 80

containers:
  - name: web
    image: registry.tourde.cloud/seppuka/web
    port: 80

build:
  - name: web
    context: .
    dockerfile: ./Dockerfile
